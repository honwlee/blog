<li class="list-item" title="${title}">
	<dl>
		<dt>
			<span class="title" title="${title}"><i class="${fAClass}"></i>${title}----${postId}</span>
			<span class="date" title="${date}">${date}</span>
		</dt>
		<dd>
			<span class="tags" title="${tags}"><i class="${tagClass}"></i>${tags}</span>
		</dd>
	</dl>
</li>